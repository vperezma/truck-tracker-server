const path = require('path')
const serverPath = 'server'

module.exports = {
    'config': path.resolve('config', 'config.json'),
    'migrations-path': path.resolve(serverPath, 'migrations'),
    'models-path': path.resolve(serverPath, 'models'),
    'seeders-path': path.resolve(serverPath, 'seeders'),
}
